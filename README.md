Basic html form with different inputs like text, date, number in with pattern and length constraints for different inputs in tabular format.    

Data may be sent to some endpoint, like to nodeJS server